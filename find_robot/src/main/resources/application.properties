server.port=8080
server.address=0.0.0.0
spring.datasource.url=jdbc:mysql://localhost:3306/lwz
spring.datasource.username=root
spring.datasource.password=liwenzhuo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

mybatis-plus.mapper-locations=classpath*:mapper/**/*.xml
mybatis-plus.type-aliases-package=com.example.demo.entity

# 静态资源只从 static/ 目录加载
spring.web.resources.static-locations=classpath:/static/
spring.mvc.static-path-pattern=/**

# Jetson 摄像头流地址
camera.stream.url=http://localhost:8081

# Jetson rosbridge WebSocket 地址
rosbridge.url=ws://localhost:9090

# Jetson 对外 IP（前端用于直接访问流，多个用逗号分隔）
jetson.ip=192.168.1.104,10.234.236.100

# ── 邮件配置（QQ邮箱SMTP）────────────────────────────────
spring.mail.host=smtp.qq.com
spring.mail.port=465
spring.mail.username=3397214850@qq.com
# 填QQ邮箱SMTP授权码（16位），不是QQ密码
# 获取：QQ邮箱网页 → 设置 → 账户 → 开启SMTP服务 → 生成授权码
spring.mail.password=rbrlyvoyahqqcjca
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.ssl.required=true
# 验证码有效期（分钟）
email.code.expire=5
